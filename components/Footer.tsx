import React from 'react'
import footerStyle from '../styles/footer.module.css'

const Footer = () => {
    return (
        <footer className={`${footerStyle.footer} is-link`}>
            <p>Footer</p>
        </footer>

    )
}

export default Footer
